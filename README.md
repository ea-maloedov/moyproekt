# МойПроект

White-label клиентский портал для IT-агентств.

## Структура

- `landing/` — статический HTML лендинг
- `app/` — Vue 3 + Vite (фронтенд)
- `api/` — NestJS (бэкенд)

## Запуск
```bash
docker compose up -d
```